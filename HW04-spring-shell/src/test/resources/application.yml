application:
  locale:
    language: ru
    basenames: /i18n/bundle
    encoding-default: UTF-8
  questions:
    path: questions/questions_%s.csv


spring:
  shell:
    interactive:
      enabled: false